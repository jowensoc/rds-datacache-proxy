# Copyright 2025 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "backend.conf"

appName:"rds-datacache-proxy"

# This is the main configuration file for the application.
# ~~~~~

# Default http client
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.AuthModule"

# Play Modules
play.modules.enabled += "uk.gov.hmrc.rdsdatacacheproxy.Module"

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes

# Microservice specific config

microservice {
  services {
    auth {
      host = localhost
      port = 8500
    }
    rds {
      host = localhost
      port = 8501
    }
  }
}

feature-switch {
    rds-stubbed = true
}

database.dispatcher {
  executor = "thread-pool-executor"
  throughput = 1
  thread-pool-executor {
    fixed-pool-size = 30
  }
}

ndds {
    firstRecord = 0
    maxRecords = 99
}

agent {
    clientList {
        gracePeriod = 14400 
    }
}

# override these, not db.default.xyz
oracle {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "ndds_data"
  password = "ndds_data"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.cis {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "cis_file_data"
  password = "cis_file_data"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.nova {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "nova_data"
  password = "nova_data"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.gambling {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "mgd_data"
  password = "mgd_data"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.gambling.gtr {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "GTR_DATA"
  password = "GTR_DATA"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.charities {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "CHARITIES_DATA"
  password = "CHARITIES_DATA"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.euvat {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "euvat_data"
  password = "euvat_data"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

oracle.ct-core {
  db.host = "localhost"
  db.port = "1521"
  dbName = "xe"
  username = "ctportal_data"
  password = "ctportal_data"
  connectionTimeout = 50s
  validationTimeout = 3s
  idleTimeout = 10m
  maximumPoolSize = 10
  protocol = TCP
  sslServerCertDn = "N/A"
  connectionType = SID
}

# NDDS DB config as named pool
db {
  default {
    url = """jdbc:oracle:thin:@
        ( DESCRIPTION=
          ( ADDRESS_LIST=
            (FAILOVER=ON)
            (LOAD_BALANCE=ON)
            ( ADDRESS=
              (PROTOCOL="""${oracle.protocol}""")
              (HOST="""${oracle.db.host}""")
              (PORT="""${oracle.db.port}""")
            )
          )
          (CONNECT_DATA=
            (SERVER=DEDICATED)
            ("""${oracle.connectionType}"""="""${oracle.dbName}""")
          )
          (SECURITY=
            (SSL_SERVER_CERT_DN="""${oracle.sslServerCertDn}""")
          )
        )"""
    driver = "oracle.jdbc.OracleDriver"
    username = ${oracle.username}
    password = ${oracle.password}
    hikaricp {
      connectionTimeout = ${oracle.connectionTimeout}
      validationTimeout = ${oracle.validationTimeout}
      minimumIdle = 1
      maximumPoolSize = ${oracle.maximumPoolSize}
      idleTimeout = ${oracle.idleTimeout}
      maxLifetime = 1h
      poolName = "ndds_portaldbPool"
      connectionInitSql = "ALTER SESSION SET NLS_TERRITORY = 'UNITED KINGDOM'"
    }
  }

  # Add Nova as a named pool
  nova {
    url = """jdbc:oracle:thin:@
        ( DESCRIPTION=
          ( ADDRESS_LIST=
            (FAILOVER=ON)
            (LOAD_BALANCE=ON)
            ( ADDRESS=
              (PROTOCOL="""${oracle.nova.protocol}""")
              (HOST="""${oracle.nova.db.host}""")
              (PORT="""${oracle.nova.db.port}""")
            )
          )
          (CONNECT_DATA=
            (SERVER=DEDICATED)
            ("""${oracle.nova.connectionType}"""="""${oracle.nova.dbName}""")
          )
          (SECURITY=
            (SSL_SERVER_CERT_DN="""${oracle.nova.sslServerCertDn}""")
          )
        )"""
    driver = "oracle.jdbc.OracleDriver"
    username = ${oracle.nova.username}
    password = ${oracle.nova.password}
    hikaricp {
      connectionTimeout = ${oracle.nova.connectionTimeout}
      validationTimeout = ${oracle.nova.validationTimeout}
      minimumIdle = 1
      maximumPoolSize = ${oracle.nova.maximumPoolSize}
      idleTimeout = ${oracle.nova.idleTimeout}
      maxLifetime = 1h
      poolName = "nova_portaldb"
      connectionInitSql = "ALTER SESSION SET NLS_TERRITORY = 'UNITED KINGDOM'"
    }
  }

# Add gambling as a named pool
    gambling {
      url = """jdbc:oracle:thin:@
          ( DESCRIPTION=
            ( ADDRESS_LIST=
              (FAILOVER=ON)
              (LOAD_BALANCE=ON)
              ( ADDRESS=
                (PROTOCOL="""${oracle.gambling.protocol}""")
                (HOST="""${oracle.gambling.db.host}""")
                (PORT="""${oracle.gambling.db.port}""")
              )
            )
            (CONNECT_DATA=
              (SERVER=DEDICATED)
              ("""${oracle.gambling.connectionType}"""="""${oracle.gambling.dbName}""")
            )
            (SECURITY=
              (SSL_SERVER_CERT_DN="""${oracle.gambling.sslServerCertDn}""")
            )
          )"""
      driver = "oracle.jdbc.OracleDriver"
      username = ${oracle.gambling.username}
      password = ${oracle.gambling.password}
      hikaricp {
        connectionTimeout = ${oracle.gambling.connectionTimeout}
        validationTimeout = ${oracle.gambling.validationTimeout}
        minimumIdle = 1
        maximumPoolSize = ${oracle.gambling.maximumPoolSize}
        idleTimeout = ${oracle.gambling.idleTimeout}
        maxLifetime = 1h
        poolName = "gambling_portaldb"
        connectionInitSql = "ALTER SESSION SET NLS_TERRITORY = 'UNITED KINGDOM'"
      }
    }

    "gambling.gtr" {
      url = """jdbc:oracle:thin:@
          ( DESCRIPTION=
            ( ADDRESS_LIST=
              (FAILOVER=ON)
              (LOAD_BALANCE=ON)
              ( ADDRESS=
                (PROTOCOL="""${oracle.gambling.gtr.protocol}""")
                (HOST="""${oracle.gambling.gtr.db.host}""")
                (PORT="""${oracle.gambling.gtr.db.port}""")
              )
            )
            (CONNECT_DATA=
              (SERVER=DEDICATED)
              ("""${oracle.gambling.gtr.connectionType}"""="""${oracle.gambling.gtr.dbName}""")
            )
            (SECURITY=
              (SSL_SERVER_CERT_DN="""${oracle.gambling.gtr.sslServerCertDn}""")
            )
          )"""
      driver = "oracle.jdbc.OracleDriver"
      username = ${oracle.gambling.gtr.username}
      password = ${oracle.gambling.gtr.password}
      hikaricp {
        connectionTimeout = ${oracle.gambling.gtr.connectionTimeout}
        validationTimeout = ${oracle.gambling.gtr.validationTimeout}
        minimumIdle = 1
        maximumPoolSize = ${oracle.gambling.gtr.maximumPoolSize}
        idleTimeout = ${oracle.gambling.gtr.idleTimeout}
        maxLifetime = 1h
        poolName = "gambling_gtr_portaldb"
        connectionInitSql = "ALTER SESSION SET NLS_TERRITORY = 'UNITED KINGDOM'"
      }
    }

  # Add CIS as a named pool
  cis {
    url = """jdbc:oracle:thin:@
        ( DESCRIPTION=
          ( ADDRESS_LIST=
            (FAILOVER=ON)
            (LOAD_BALANCE=ON)
            ( ADDRESS=
              (PROTOCOL="""${oracle.cis.protocol}""")
              (HOST="""${oracle.cis.db.host}""")
              (PORT="""${oracle.cis.db.port}""")
            )
          )
          (CONNECT_DATA=
            (SERVER=DEDICATED)
            ("""${oracle.cis.connectionType}"""="""${oracle.cis.dbName}""")
          )
          (SECURITY=
            (SSL_SERVER_CERT_DN="""${oracle.cis.sslServerCertDn}""")
          )
        )"""
    driver = "oracle.jdbc.OracleDriver"
    username = ${oracle.cis.username}
    password = ${oracle.cis.password}
    hikaricp {
      connectionTimeout = ${oracle.cis.connectionTimeout}
      validationTimeout = ${oracle.cis.validationTimeout}
      minimumIdle = 1
      maximumPoolSize = ${oracle.cis.maximumPoolSize}
      idleTimeout = ${oracle.cis.idleTimeout}
      maxLifetime = 1h
      poolName = "cis_portaldb"
    }
  }

  # Add CHARITIES as a named pool
  charities {
    url = """jdbc:oracle:thin:@
        ( DESCRIPTION=
          ( ADDRESS_LIST=
            (FAILOVER=ON)
            (LOAD_BALANCE=ON)
            ( ADDRESS=
              (PROTOCOL="""${oracle.charities.protocol}""")
              (HOST="""${oracle.charities.db.host}""")
              (PORT="""${oracle.charities.db.port}""")
            )
          )
          (CONNECT_DATA=
            (SERVER=DEDICATED)
            ("""${oracle.charities.connectionType}"""="""${oracle.charities.dbName}""")
          )
          (SECURITY=
            (SSL_SERVER_CERT_DN="""${oracle.charities.sslServerCertDn}""")
          )
        )"""
    driver = "oracle.jdbc.OracleDriver"
    username = ${oracle.charities.username}
    password = ${oracle.charities.password}
    hikaricp {
      connectionTimeout = ${oracle.charities.connectionTimeout}
      validationTimeout = ${oracle.charities.validationTimeout}
      minimumIdle = 1
      maximumPoolSize = ${oracle.charities.maximumPoolSize}
      idleTimeout = ${oracle.charities.idleTimeout}
      maxLifetime = 1h
      poolName = "charities_filedb"
    }
  }

  # Add EUVAT as a named pool
  euvat {
    url = """jdbc:oracle:thin:@
        ( DESCRIPTION=
          ( ADDRESS_LIST=
            (FAILOVER=ON)
            (LOAD_BALANCE=ON)
            ( ADDRESS=
              (PROTOCOL="""${oracle.euvat.protocol}""")
              (HOST="""${oracle.euvat.db.host}""")
              (PORT="""${oracle.euvat.db.port}""")
            )
          )
          (CONNECT_DATA=
            (SERVER=DEDICATED)
            ("""${oracle.euvat.connectionType}"""="""${oracle.euvat.dbName}""")
          )
          (SECURITY=
            (SSL_SERVER_CERT_DN="""${oracle.euvat.sslServerCertDn}""")
          )
        )"""
    driver = "oracle.jdbc.OracleDriver"
    username = ${oracle.euvat.username}
    password = ${oracle.euvat.password}
    hikaricp {
      connectionTimeout = ${oracle.euvat.connectionTimeout}
      validationTimeout = ${oracle.euvat.validationTimeout}
      minimumIdle = 1
      maximumPoolSize = ${oracle.euvat.maximumPoolSize}
      idleTimeout = ${oracle.euvat.idleTimeout}
      maxLifetime = 1h
      poolName = "euvat-datacachedb"
    }
  }

  # Add CT-Core as a named pool
  ct-core {
    url = """jdbc:oracle:thin:@
        ( DESCRIPTION=
          ( ADDRESS_LIST=
            (FAILOVER=ON)
            (LOAD_BALANCE=ON)
            ( ADDRESS=
              (PROTOCOL="""${oracle.ct-core.protocol}""")
              (HOST="""${oracle.ct-core.db.host}""")
              (PORT="""${oracle.ct-core.db.port}""")
            )
          )
          (CONNECT_DATA=
            (SERVER=DEDICATED)
            ("""${oracle.ct-core.connectionType}"""="""${oracle.ct-core.dbName}""")
          )
          (SECURITY=
            (SSL_SERVER_CERT_DN="""${oracle.ct-core.sslServerCertDn}""")
          )
        )"""
    driver = "oracle.jdbc.OracleDriver"
    username = ${oracle.ct-core.username}
    password = ${oracle.ct-core.password}
    hikaricp {
      connectionTimeout = ${oracle.ct-core.connectionTimeout}
      validationTimeout = ${oracle.ct-core.validationTimeout}
      minimumIdle = 1
      maximumPoolSize = ${oracle.ct-core.maximumPoolSize}
      idleTimeout = ${oracle.ct-core.idleTimeout}
      maxLifetime = 1h
      poolName = "ct-core_filedb"
    }
  }

}
